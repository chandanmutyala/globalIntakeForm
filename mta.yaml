_schema-version: "3.2"
ID: globalintakeform
description: Generated by Fiori Tools
version: 0.0.1
modules:
- name: globalintakeform-destination-content
  type: com.sap.application.content
  requires:
  - name: globalintakeform-destination-service
    parameters:
      content-target: true
  - name: globalintakeform-repo-host
    parameters:
      service-key:
        name: globalintakeform-repo-host-key
  - name: globalintakeform-uaa
    parameters:
      service-key:
        name: globalintakeform-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: globalintakeform_html_repo_host
          ServiceInstanceName: globalintakeform-html5-srv
          ServiceKeyName: globalintakeform-repo-host-key
          sap.cloud.service: globalintakeform
        - Authentication: OAuth2UserTokenExchange
          Name: globalintakeform_uaa
          ServiceInstanceName: globalintakeform-xsuaa-srv
          ServiceKeyName: globalintakeform-uaa-key
          sap.cloud.service: globalintakeform
        existing_destinations_policy: ignore
  build-parameters:
    no-source: true
- name: globalintakeform-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: globalintakeform-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - globalintakeform.zip
      name: globalintakeform
      target-path: resources/
- name: globalintakeform
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: globalintakeform-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: globalintakeform-destination-service
    service-plan: lite
- name: globalintakeform-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: globalintakeform-xsuaa-srv
    service-plan: application
- name: globalintakeform-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: globalintakeform-html5-srv
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
